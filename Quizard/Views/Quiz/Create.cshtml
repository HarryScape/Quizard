@model Quizard.ViewModels.CreateQuizViewModel

<script src = "/js/site.js" defer></script>

<h1> @Model.Quiz.QuizName </h1>
<br />

@{
    var i = 1;
}
<div class="QuestionAccordian">
    <div class="panel-group" id="accordionY">
        @foreach (var question in Model.Questions)
        {
            <div class="accordion accordion-flush" id="accordionFlushExample" draggable="true">
                <div class="accordion-item">
                    <h2 class="accordion-header" id="flush-headingOne">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false" aria-controls="flush-collapseOne">
                            <p>@question.QuestionTitle</p>
                        </button>
                    </h2>
                    <div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accordionFlushExample">
                        <div class="accordion-body">
                            @foreach (var answer in Model.Answers.Where(i => i.QuestionId.Equals(question.Id)))
                            {
                                @*<p>@answer.QuestionAnswer</p>*@

                                @if (answer.isCorrect.Contains("Correct"))
                                {
                                    <strong>@answer.QuestionAnswer</strong>
                                }
                                else
                                {
                                    @answer.QuestionAnswer
                                }
                                <br />
                            }
                        </div>
                    </div>
                </div>
            </div>
            i++;
        }
    </div>
</div>











@*acordian test*@

@*<br /><br /><br />


@{
    var counter = 1;
}

<div class="bs-example" id="AccordianList">
    <div class="panel-group" id="accordionY">
        @{
            int ij = 1;
        }
        @foreach (var question in Model.Questions)
        {
            <div class="row">
                <div class="col">
                    <div class="tabs">
                        <div class="tab">
                            <input type="checkbox" id=@ij>
                            <label class="tab-label" for=@ij>Question @counter: @question.QuestionTitle</label>
                            <div class="tab-content">

                                @foreach (var answer in Model.Answers.Where(i => i.QuestionId.Equals(question.Id)))
                                {
                                    @if (answer.isCorrect.Contains("Correct"))
                                    {
                                        <strong>@answer.QuestionAnswer</strong>
                                    }
                                    else
                                    {
                                        @answer.QuestionAnswer
                                    }
                                    <br />
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            ij++;
            counter++;
        }
    </div>
</div>*@





@*JQuery drag and drop*@


<div class="container">
    <div class="col-md-6">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>TestTest</th>
                </tr>
            </thead>
            <tbody id="sortable" style="cursor:pointer;">
                @foreach (var question in Model.Questions)
                {
                    <tr>
                        <td>@question.QuestionTitle</td>
                        <td class="taskSingleInLine" id="task@(question.Id)" data-taskid="@(question.Id)">@question.QuestionTitle</td>
                    </tr>
                }
            </tbody>
        </table>
        <a class="btn btn-primary" href="@Url.Action("Save", "Quiz")">Save</a>
    </div>
</div>